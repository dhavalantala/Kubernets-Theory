apiVersion: v1 
kind: Service 
metadata: 
  name: backend-svc
  namespace: dev-backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8080          # wget -O- backend-svc.dev-backend.svc.cluster.local:8080
      targetPort: 5678

      